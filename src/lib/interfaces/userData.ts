/**
 * Represents the user Data.
 *
 * @interface UserData
 * @property {string} userName - The full user name.
 * @property {string} email - The email address of the user.
 */
export interface UserData {
  userName: string;
  email: string;
}
